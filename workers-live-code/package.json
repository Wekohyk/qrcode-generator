{
  "name": "weko-qr-live",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute weko-qr --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute weko-qr --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
